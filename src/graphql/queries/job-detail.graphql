#import "../fragments/job-query-fragment.graphql"

query jobDetail($id: String!) {
	job(id: $id) {
		...JobQueryFragment
		recruiter {
			id
			name
			email
			phone
		}
		organization {
			id
			name
			website
			linkedinUrl
			logo
		}
	}
}
