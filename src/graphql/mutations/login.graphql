mutation login($loginInput: LoginInput!) {
	login(loginInput: $loginInput) {
		expiresAt
		tokenType
		accessToken
		refreshToken
		user {
			id
			name
			role
			isActive
			isEmailVerified
			organization {
				id
			}
		}
	}
}
